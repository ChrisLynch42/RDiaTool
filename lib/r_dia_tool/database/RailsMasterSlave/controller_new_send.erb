    <%= prepare_comment_above('SEND NEW') %>    
    url_params = { 'letter' => params[:letter] }
    respond_to do |format|
      new_html(format, @<%= single %>,url_params)
      format.json { render json: @<%= single %> }
    end
    <%= prepare_comment_below('SEND NEW') %>    
      
